This directory contains the input files used for different occasions like
demonstrations.

Be aware that these files might not be usable anymore because the input
file syntax has changed since the date of their creation. 
